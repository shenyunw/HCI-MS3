SITE
- Make CSS/library files local
BROWSE
x Rating system
X add more pizzas
X link the "select" button to the build + transaction page
    (when a menu pizza is selected, update the build with preloaded ingredients, also on transaction page too)

X clean up style of browse items (text color, body color)

BUILD
X add ingredients
X add icons for the ingredients
X move the entire container so its not cut off
X make each tab different colours and the topping bodies matching its corresponding tab colour


TRANSACTION PAGE
X create one..
X have a pizza that the person is ordering appear here as well
    X updates across each page during pizza selection + build process


HOME PAGE
X what to put here

TODO fix orders page so it doesnt scale the whole site